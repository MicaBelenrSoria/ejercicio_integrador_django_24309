# En esta cuarto ejercicio hay que  Escribir una función que reciba una cadena de caracteres y devuelva un diccionario con cada palabra que contiene y la cantidad de veces que aparece (frecuencia). Escribir otra función que reciba el diccionario generado con la función anterior y devuelva una tupla con la palabra más repetida y su frecuencia.

# En primer lugar desarrollo una funcion 'contar_palabras'

# En la cual recibe una cadena de caracteres y devuelve un diccionario con la frecuencia de cada palabra.Y convierte la cadena a minúsculas y elimina los signos de puntuación.
# Luego divide la cadena en palabras y actualiza el diccionario con la frecuencia de cada palabra.

# En segundo lugar creo una función 'palabra_mas_repetida'
# En la cual recibe un diccionario de frecuencias de palabras y devuelve una tupla con la palabra más repetida y su frecuencia.
# Y utiliza la función 'max' para encontrar la palabra con la mayor frecuencia.

# En tercer lugar  creo un programa principal
# Que solicita una cadena de caracteres al usuario.
# Tambien utiliza la función 'contar_palabras' para obtener el diccionario de frecuencias.
# Y utiliza la función 'palabra_mas_repetida' para encontrar la palabra más repetida y su frecuencia.
# Por ultimo, imprime los resultados.

#Mi objetivo con con este programa, puedes ingresar una cadena de caracteres, y asi obtener la frecuencia de cada palabra y encontrar la palabra más repetida junto con su frecuencia. 